# Frontend (MVC)

This is a minimal static frontend following an MVC pattern. Files are served by the Express server.

Structure:
- `js/models` – data access (API calls)
- `js/views` – DOM rendering
- `js/controllers` – app flow

To run: start the backend and open http://localhost:5000
